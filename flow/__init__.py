"""
The steps every version runs.

The order they happen in is pipeline.py, the five checks are checks.py,
and writing PRINTED back is update_status.py. None of it knows whether
the records came from Excel or the database.
"""
